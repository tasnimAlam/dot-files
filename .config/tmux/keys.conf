# Key bindings.

# Prefix: C-Space, with C-\ as a secondary prefix.
unbind C-b
set -g prefix C-Space
bind-key C-Space send-prefix

unbind C-\\
set -g prefix2 C-\\
bind-key C-\\ send-prefix

# Reload
bind r source-file ~/.config/tmux/tmux.conf \; display 'Tmux reloaded'

# Session / window picker
unbind w
bind w choose-tree -ZsN

# Jump to the marked pane. Deliberately not on q -- q is display-panes, which
# is more useful more often. Pairs with the default m (select-pane -m).
bind M switch-client -t'{marked}'

# Splits and new windows inherit the current pane's cwd
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind c new-window -c "#{pane_current_path}"
unbind '"'
unbind %

# Toggle synchronised panes
bind C-s set-window-option synchronize-panes

# Copy mode (Wayland clipboard). Paste is the default <prefix> ] -- don't bind
# P, tmux-floax claims it later in plugins.conf and would silently win.
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'wl-copy'
bind -T copy-mode-vi C-v send -X rectangle-toggle
bind -T copy-mode-vi Escape send -X cancel
bind -T copy-mode-vi H send -X start-of-line
bind -T copy-mode-vi L send -X end-of-line

# Mouse-drag selection should reach the Wayland clipboard too, and should not
# jump the view back to the bottom on release.
bind -T copy-mode-vi MouseDragEnd1Pane send -X copy-pipe-no-clear 'wl-copy'

# Pane selection is on C-h/C-j/C-k/C-l with no prefix (see below), so <prefix>
# h/j/k/l is left unbound -- that keeps <prefix> l as last-window. Unbind
# explicitly: source-file only adds binds, it never drops ones a previous
# reload installed.
unbind h
unbind j
unbind k
bind l last-window
bind q display-panes

# Window selection: no prefix
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9

# Reorder windows
bind -r "<" swap-window -d -t -1
bind -r ">" swap-window -d -t +1

# Resize panes: no prefix, hold Alt+Shift and tap
bind -n M-H resize-pane -L 2
bind -n M-J resize-pane -D 2
bind -n M-K resize-pane -U 2
bind -n M-L resize-pane -R 2

# Window navigation: no prefix
bind -n M-h previous-window
bind -n M-l next-window

# Kill pane / session
unbind-key x
bind-key x kill-pane
bind-key X confirm-before -p "kill-session #S? (y/n)" kill-session

# Clear screen (prefix C-l, since C-l is taken by pane navigation below)
bind C-l send-keys 'C-l'

# vim-tmux-navigator style pane movement: pass the key through when the pane
# is running Vim, otherwise move panes.
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"

bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h' 'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j' 'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k' 'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l' 'select-pane -R'

# NOTE: no root-table C-\ bind here -- C-\ is prefix2 (see top of file), and
# tmux matches prefix keys before the root table, so such a bind can never fire.

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l
bind-key -T copy-mode-vi 'C-Space' select-pane -t:.+

# sesh session manager
bind-key "t" run-shell "sesh connect \"$(
  sesh list --icons | fzf-tmux -p 80%,70% \
    --no-sort --ansi --border-label ' sesh ' --prompt '⚡  ' \
    --header '  ^a all ^t tmux ^g configs ^x zoxide ^d tmux kill ^f find' \
    --bind 'tab:down,btab:up' \
    --bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list --icons)' \
    --bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t --icons)' \
    --bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -c --icons)' \
    --bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)' \
    --bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
    --bind 'ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)' \
    --preview-window 'right:50%' \
    --preview 'sesh preview {}'
)\""
bind -N "last-session (via sesh)" L run-shell "sesh last"
